{
  "name": "@project-control/web",
  "version": "1.0.0",
  "private": true,
  "description": "Project Control — operator web panel (Stage 1 skeleton)",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.app.json && tsc -p tsconfig.node.json && vite build",
    "typecheck": "tsc -p tsconfig.app.json && tsc -p tsconfig.node.json",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@project-control/contracts": "workspace:*",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.18.3"
  },
  "devDependencies": {
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitejs/plugin-react": "6.0.5",
    "typescript": "5.9.3",
    "vite": "8.2.0",
    "vitest": "4.1.10"
  }
}
